"""Core domain packages for declaration, discovery, and governed execution.

Each package owns its public contracts. Import from the relevant domain rather
than a second umbrella API; common application entry points live in ``conducto``.
"""
